# Movie Recommendation System

A simple movie recommendation and review project built with **Spring Boot**, **PostgreSQL**, and a **Python FastAPI** recommender service.  
Everything runs smoothly using **Docker Compose**, so you can start the whole system with one command.

---

## How to Run

Make sure you have **Docker** and **Docker Compose** installed, then run:

```bash
docker compose up --build
